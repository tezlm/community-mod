Vars.content.blocks().each(block => block.buildVisibility = BuildVisibility.shown)
Vars.content.blocks().each(block => block.alwaysReplace = true)
//this is very cursed to play with, you should play with it
